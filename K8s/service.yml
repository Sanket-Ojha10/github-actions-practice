apiVersion:  v1

kind: Service

metadata: 
  name:   github-actions-cicd-service

spec:  
  type: NodePort
  selector: 
    app: github-actions-cicd

  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
